# Project Integration Tests

Editor-only automation harness for cross-plugin integration scenarios. The plugin owns long-running PIE flows and provides a home for future simulated end-to-end checks. Additional modules can be added for specialized domains (e.g., networking, saves) without impacting runtime builds.
