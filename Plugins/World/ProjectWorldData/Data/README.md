# ProjectWorldData

This directory owns production Kazan source and derived world data. The
`Manifests/` child is a strict durable authority root and remains empty until
the first accepted enrollment publishes its active set and immutable scopes.
