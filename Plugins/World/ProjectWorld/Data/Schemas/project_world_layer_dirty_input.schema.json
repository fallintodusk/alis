{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alis.world/schemas/project-world/layer-dirty-input-v1.json",
  "title": "ProjectWorld Layer Dirty Operation Input",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schema_version", "realization_profile_id", "base_layers", "operator_additions"],
  "properties": {
    "$schema": {"type": "string", "pattern": "project_world_layer_dirty_input\\.schema\\.json$"},
    "schema_version": {"const": 1},
    "realization_profile_id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
    "base_layers": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["layer_id", "normalized_layer_contract_sha256", "canonical_inputs"],
        "properties": {
          "layer_id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
          "normalized_layer_contract_sha256": {"$ref": "#/$defs/sha256"},
          "canonical_inputs": {
            "type": "array",
            "items": {"$ref": "#/$defs/input"}
          }
        }
      }
    },
    "operator_additions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["layer_id", "units"],
        "properties": {
          "layer_id": {"type": "string", "pattern": "^[a-z0-9_]+$"},
          "units": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"type": "string", "minLength": 1}
          }
        }
      }
    }
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "input": {
      "type": "object",
      "additionalProperties": false,
      "required": ["unit_id", "sha256"],
      "properties": {
        "unit_id": {"type": "string", "minLength": 1},
        "sha256": {"$ref": "#/$defs/sha256"}
      }
    }
  }
}
