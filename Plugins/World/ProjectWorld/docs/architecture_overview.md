# World Reconstruction Architecture and Observability

First-read visual map for operators and fresh agents. Mermaid renders inline
with no separate architecture service. This is a navigation view, not another
SOT; exact behavior stays with linked owners. Update an owner and this view
together. World work does not require the project-wide Structurizr workspace.

Maintenance: use basic Mermaid `flowchart` syntax only. Do not add custom
themes, layout state, generated images, or click actions. Keep detailed rules
with their real owner and keep links valid.

## 1. Thirty-second golden path

```mermaid
flowchart LR
    REAL["Real-world sources"]
    SRC["Verify sources and admit territory contracts"]
    CANON["Canonical world authority"]
    UE["Generated Unreal world"]
    AUTH["Active manifest authority"]
    BUILD["Audited packaged game"]
    RUN["World Partition runtime"]
    TEST["Check and Matrix evidence"]

    REAL --> SRC --> CANON --> UE --> AUTH --> BUILD --> RUN
    TEST -.-> SRC
    TEST -.-> CANON
    TEST -.-> UE
```

Human-edited tracked inputs define intent; promoted canonical data is portable
geographic and regeneration authority; Unreal packages are derived; active
manifests authorize exact generated bytes; packaged runtime consumes only
audited accepted state.

| Authority | Exact role |
|---|---|
| Geographic and regeneration authority | `Data/Canonical/<profile_id>/active.json` plus its immutable bundle |
| Serialized Unreal authority | `Content/Generated/` and external packages authorized by `Data/Manifests/active_set.json` |

## 2. Ownership and dependency direction

```mermaid
flowchart LR
    OP["Operator or agent"]

    subgraph DATA["Concrete data owners"]
        PROD["ProjectWorldData: Kazan inputs and durable outputs"]
        TEST["ProjectWorldTestData: editor-only fixtures"]
    end

    subgraph TOOLS["Reusable engine-independent logic"]
        ENV["ExecutionEnvironment"]
        SRC["SourceIngestion"]
        COMP["CanonicalCompilation"]
        E2E["EndToEndValidation"]
    end

    subgraph UE["Reusable Unreal logic"]
        PW["ProjectWorld editor and runtime modules"]
        WRAP["World realization and authority scripts"]
    end

    subgraph PRODUCT["Persistent and shipped representations"]
        CANON["Canonical JSON authority and materialized base"]
        PACKAGES["Generated maps and external packages"]
        MANIFESTS["Active immutable manifests"]
        SHIP["Locked package and rendered gate"]
        RUNTIME["World Partition packaged runtime"]
    end

    OP --> E2E
    PROD --> SRC
    TEST --> SRC
    ENV --> SRC
    SRC --> COMP
    ENV --> COMP
    PROD --> COMP
    TEST --> COMP
    COMP --> WRAP
    PW --> WRAP
    PROD --> WRAP
    TEST --> WRAP
    E2E --> SRC
    E2E --> COMP
    E2E --> WRAP
    COMP --> CANON
    WRAP --> PACKAGES
    WRAP --> MANIFESTS
    CANON --> WRAP
    E2E --> SHIP
    PACKAGES --> SHIP
    MANIFESTS --> SHIP
    SHIP --> RUNTIME
```

Arrows show consumption, not ownership transfer. `ProjectWorld` owns reusable
Unreal interpretation; each data plugin owns its concrete inputs and outputs.
`EndToEndValidation` composes public commands and receipts without
reimplementing a stage.

| Boundary | Owner link |
|---|---|
| Reproducible Python and native tools | [Execution Environment](../../../../tools/World/ExecutionEnvironment/README.md) |
| Provider rights, acquisition, coverage, and source receipts | [Source Ingestion](../../../../tools/World/SourceIngestion/README.md) |
| Grid, cells, canonical identities, admission, and promotion | [Canonical Compilation](../../../../tools/World/CanonicalCompilation/README.md) |
| Unreal realization and runtime interpretation | [ProjectWorld](../README.md) |
| Kazan profiles and durable output | [ProjectWorldData](../../ProjectWorldData/README.md) |
| Synthetic profiles and durable fixtures | [ProjectWorldTestData](../../ProjectWorldTestData/README.md) |
| Cross-stage evidence and package acceptance | [End-to-End Validation](../../../../tools/World/EndToEndValidation/README.md) |

## 3. Data and persistence lifecycle

```mermaid
flowchart TD
    SOURCE_PROFILE["Tracked source profile and licence pins"]
    COMPILE_PROFILE["Tracked compile profile"]
    CONTROL["Tracked control network"]
    BUDGET["Tracked hard budget"]
    OVERLAY["Tracked authored overlay"]

    PLAN["Plan, acquire, verify, and receipt source"]
    RASTER["Normalized raster components and alpha coverage mosaic"]
    OSM["Complete OSM geometry membership and referential closure"]
    ADMIT["Admit owner, CRS, coverage, controls, and budget"]
    WATER["Profile water admission, exact groups, width, and surface Z"]
    COMPILE["Deterministic canonical compile"]
    CELLS["Canonical cells with touched raster pixels and feature lineage"]
    PROOF["D0, D1, and D2 deterministic proof"]
    CANDIDATE["Ignored candidate tree and compile receipt"]
    MATRIX["L2 Matrix: prove semantics and restore UE tree"]
    PROMOTE["Promote authenticated canonical result"]
    BUNDLE["Data/Canonical profile authority"]
    MATERIALIZE["Materialize authenticated accepted base"]
    L3["L3 operator-controlled Unreal realization"]
    UE_TREE["Content/Generated plus external packages"]
    ACTIVE["Data/Manifests/active_set.json"]
    L4["L4 locked audit, package, render, and post-audit"]

    SOURCE_PROFILE --> PLAN
    SOURCE_PROFILE --> ADMIT
    COMPILE_PROFILE --> ADMIT
    CONTROL --> ADMIT
    BUDGET --> ADMIT
    PLAN --> RASTER
    PLAN --> OSM
    RASTER --> COMPILE
    OSM --> COMPILE
    RASTER --> WATER
    OSM --> WATER
    COMPILE_PROFILE --> WATER
    WATER --> COMPILE
    ADMIT --> COMPILE
    COMPILE_PROFILE --> COMPILE
    OVERLAY --> COMPILE
    COMPILE --> CELLS
    CELLS --> PROOF
    PROOF --> CANDIDATE
    CANDIDATE --> MATRIX
    CANDIDATE --> PROMOTE
    ADMIT --> PROMOTE
    PROMOTE --> BUNDLE
    BUNDLE --> MATERIALIZE
    MATERIALIZE --> L3
    MATRIX -->|accepted evidence| L3
    L3 --> UE_TREE
    L3 --> ACTIVE
    UE_TREE --> L4
    ACTIVE --> L4
```

Persistent derived layers:

1. `Data/Canonical/<profile_id>/` holds the promoted portable bundle and
   `active.json`.
2. `Content/Generated/` plus `Data/Manifests/` holds the L3 Unreal
   representation and authority.

`tmp/world/` candidates and `Saved/Validation/` evidence are not editable
authority. Matrix may regenerate UE packages but restores the exact pre-run
tree.

## 4. Safe Unreal regeneration transaction

```mermaid
flowchart TD
    START["Exact accepted tree or explicitly absent reconstruction scope"]
    LOCK["Acquire project content lock and owner authority lock"]
    PREFLIGHT["Verify active set, manifests, ownership, and drift"]
    PROFILE["Validate exact generator tuples and strict layer roots"]
    DIRTY["Resolve typed current/prior units and clipped dependency closure"]
    SNAPSHOT["Snapshot exact map, external, layer, and presentation scopes"]
    CHILD["Run ProjectWorld realization commandlet"]
    INVENTORY["Verify exact layer artifacts and per-cell water lineage"]
    RECEIPT["Validate structural, geospatial, runtime, and zero-HLOD receipt"]
    CLEAN["Remove only obsolete exact-owned companions"]
    CANDIDATES["Write immutable candidate manifests"]
    COMMIT["Atomically replace active_set.json: durable commit point"]
    AUDIT["Post-commit read-only authority audit"]
    ELIGIBLE["Eligible for packaging"]
    BLOCK["Block packaging; require explicit repair or regeneration"]
    REFUSE["Refuse without mutation"]
    RESTORE["Restore snapshot and preserve primary failure"]

    START --> LOCK
    LOCK --> PREFLIGHT
    PREFLIGHT --> PROFILE --> DIRTY --> SNAPSHOT
    SNAPSHOT --> CHILD
    CHILD --> INVENTORY --> RECEIPT
    RECEIPT --> CLEAN
    CLEAN --> CANDIDATES
    CANDIDATES --> COMMIT
    COMMIT --> AUDIT
    AUDIT -->|accepted| ELIGIBLE
    AUDIT -->|rejected| BLOCK

    PREFLIGHT -->|reject| REFUSE
    PROFILE -->|reject| REFUSE
    DIRTY -->|reject| REFUSE
    SNAPSHOT -->|failure| RESTORE
    CHILD -->|failure| RESTORE
    INVENTORY -->|reject| RESTORE
    RECEIPT -->|reject| RESTORE
    CLEAN -->|failure| RESTORE
    CANDIDATES -->|failure| RESTORE
```

The active-set replacement commits. Older manifests remain immutable
provenance. Recovery and mutation live in
[Canonical World Realization](../../../../scripts/ue/world/README.md).

## 5. Runtime representation contract

```mermaid
flowchart TD
    CANON["Accepted canonical terrain and water"]
    REF["Epic GeoReferencing"]
    GRID["Canonical metric grid and stable cell identities"]
    LAND_API["Epic LandscapeConfigHelper"]
    GEOM["GeometryCore and gated GeometryAlgorithms"]
    MESH["MeshDescription and StaticMesh"]
    NANITE["Nanite-compatible opaque geometry"]
    WATER["Non-Nanite Single Layer Water geometry"]
    WP["One World Partition world"]
    LAND["One logical Landscape with 210 component/proxy cells"]
    STATIC["Cell-local water StaticMesh and supported static geometry"]
    INST["ISM, HISM, or admitted PCG instance ownership"]
    AUTH["Protected authored and dynamic gameplay layers"]
    GAME["Collision, navigation, replication, and gameplay"]
    REJECT["HLOD, Experimental Water authority, and Experimental MeshPartition"]

    CANON --> REF
    REF --> GRID
    GRID --> WP
    GRID --> LAND_API --> LAND --> WP
    GRID --> GEOM --> MESH
    MESH --> NANITE --> STATIC
    MESH --> WATER --> STATIC --> WP
    WP --> INST
    WP --> AUTH
    LAND --> GAME
    STATIC --> GAME
    INST --> GAME
    AUTH --> GAME
    WP --> GAME
    CANON -->|rejects as authority| REJECT
```

This is reuse-first, not blind adoption. The Beta GeometryAlgorithms module is
Editor-only and admitted after the synthetic geometry, save, and reload proof;
packaged runtime consumes its saved assets and must still pass the later cook
gate. Experimental systems stay outside authority. Production generation also
requires zero HLOD layers, actors, proxies, companion packages, and eligible
generated actors. Exact API, fallback, and measurement rules live in
[World Partition](world_partition.md#ue-58-native-reuse-gate).

## 6. Proof layers and evidence flow

```mermaid
flowchart LR
    CHANGE["Change"]
    PLAN["Static plan"]
    L0["L0 exact proof"]
    L1["L1 if a seam changed"]
    CHECK["One shared Check"]
    MATRIX["Affected Matrix only"]
    COMPLETE["Slice complete"]
    L3["L3 if generated authority changes"]
    L4["L4 if shipping changes"]

    CHANGE --> PLAN
    PLAN --> L0
    L0 -->|cross-component| L1
    L0 -->|otherwise at slice boundary| CHECK
    L1 -->|at slice boundary| CHECK
    CHECK --> MATRIX
    MATRIX --> COMPLETE
    MATRIX -->|durable generated mutation| L3
    MATRIX -->|shipping boundary without L3| L4
    L3 --> COMPLETE
    L3 -->|shipping boundary| L4
    L4 --> COMPLETE
```

The planner selects the minimum route; L1, L3, and L4 are conditional. L0
through L2 leave no durable generated-world mutation. Exact escalation lives
in [World Pipeline Test Layers](../../../../docs/testing/world_pipeline_layers.md).

## 7. Operator observability surface

| Question | Read-only surface | Meaning |
|---|---|---|
| What proof does this diff require? | End-to-End Validation `plan` route | Static L0-L4 selection; no mutation. |
| What concrete inputs define Kazan? | [`ProjectWorldData/Data/`](../../ProjectWorldData/Data/README.md) | Tracked profiles, controls, overlays, budgets, and licences. |
| Which canonical bundle is active? | `ProjectWorldData/Data/Canonical/<profile_id>/active.json` | Persistent portable canonical authority after promotion. |
| Which Unreal artifacts are active? | [`ProjectWorldData/Data/Manifests/active_set.json`](../../ProjectWorldData/Data/Manifests/active_set.json) | Atomic current scope set and immutable manifest hashes. |
| What generations existed before? | Realization `show_generated_history` route | Read-only active, historical, and archived scope history. |
| Is the generated tree exact? | Realization `audit_generated_authority` route | Drift, ownership, consumer, and generator-fingerprint proof. |
| Is a mutation interrupted? | Owner `Data/Manifests/journal.json`, when present | Fail-closed pending transaction; use the documented recovery route. |
| Did the slice pass? | `Saved/Validation/WorldPipeline/<operation>/result.json` | Check or profile-scoped Matrix evidence, not tracked SOT. |
| Did shipping pass? | `Saved/Validation/WorldAuthority/<accept-id>/acceptance_chain.json` | Locked audit, package, IoStore, render, and post-audit chain. |
| What did the packaged GPU gate measure? | Acceptance `presentation_gate.json` and captures | Machine, GPU, RHI, cameras, samples, and p95 frame time. |

## 8. Fresh-agent route

1. Start here, then open only the owner link for the node being changed.
2. Open only the required [Territory Generation](territory_generation.md) SOT section.
3. Use the End-to-End Validation planner before choosing tests.
4. Keep candidates ignored; persist only through promotion or L3 realization.
5. Never edit receipts or immutable manifests to make a gate pass.
