# Project Agent Skills

This directory owns tracked, project-scoped agent instructions. Claude reads
the directories directly. Codex uses the ignored `.agents/skills` projection
created by `scripts/agents/link_codex_skills.ps1`; it is not a second authored
copy.
